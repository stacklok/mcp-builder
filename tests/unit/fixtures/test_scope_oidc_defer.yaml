version: "1"

server:
  name: test-api
  description: "Test API MCP server with OIDC, deferred external URL"

spec:
  source: "test_openapi.yaml"
  format: openapi3
  base_url: "https://api.example.com"

groups:
  - name: item-operations
    description: "Item operations"
    tools:
      - tool_name: get_item
        endpoint: GET /items/{itemId}
        description: "Get an item by its ID."
        response_kind: json
        parameters:
          - name: itemId
            description: "The item identifier."
            required: true
            location: path

# No external_url_template or client_type — deploy-assist will substitute
# REPLACE_ME_DOMAIN at deploy time and the clientSecretRef stays as a
# commented example.
auth:
  type: oidc
  issuer: "https://accounts.google.com"
  scopes_available:
    openid: "OpenID Connect sign-in"
    email: "User email"
  scopes_required:
    - openid
    - email
