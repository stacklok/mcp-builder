[project]
name = "mcp-template-py"
version = "0.1.0"
description = "Python MCP server template."
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.22.0",
    "pydantic>=2.12.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
